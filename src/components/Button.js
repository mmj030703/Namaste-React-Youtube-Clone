import React from 'react'

const Button = ({ name }) => {
    return (
        <div className='w-fit bg-[rgb(230,232,235)] px-4 py-1 rounded-lg text-[14.1px] font-semibold'>{name}</div>
    )
}

export default Button